#include<stdio.h>
#include<conio.h>
#include<string.h>
main()
{
	char cadena[10],letra,posi;
	int car;
	printf("Cadena de carcteres: ");
	gets(cadena);
	if(cadena>0)
	{
		printf("Cadena a excluir: ");
		scanf("%c",&letra);
		if(letra==0)
		{
	       posi=cadena;
           car=posi;
           if(strlen(letra)==car)
		   {
		      
    	   }
	    }
	}
}
